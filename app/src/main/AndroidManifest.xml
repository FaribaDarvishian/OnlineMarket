<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.digikala">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".OnlineMarketApplication"
        android:allowBackup="true"
        android:icon="@drawable/background_splash"
        android:label="@string/app_name"
        android:roundIcon="@drawable/background_splash"
        android:supportsRtl="true"
        android:theme="@style/theme.splashTheme">
        <service
            android:name=".services.PollJobService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE"/>
        <activity android:name=".view.activity.MainPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>